# Visual/textual fidelity — scans 129–137

Source derivative: `TVA_BOK_0064097_புதையல்_part_003_pages_99-147.pdf`

Native split pages reviewed: **31–39**  
Source scans: **129–137**  
Printed pages: **127–135**  
Result: **PASSED — 9 / 9 verified; unresolved 0**

## Baseline scope

The user supplied a baseline labelled `Pages 126 through 135`. It overlaps the already verified scan 128 / printed 126 and then supplies text through scan 136 / printed 134. The claimed final printed page 135 was absent from the supplied text.

Accordingly:

- scan 128 remains governed by its existing direct-source reconciliation;
- scans 129–136 were reconciled baseline-vs-native image;
- scan 137 / printed 135 was transcribed directly from the native image because the baseline omitted the physical page.

## Scan 128 overlap retained

The existing canonical scan 128 remains unchanged. Source-established forms include:

- `நன்றுக`, not baseline `நன்றாக`;
- `துரையும், கிழவர்—`;
- `நேரம் போக்குக்கு`;
- `நேரம் ஆடுவதும்`;
- physical endpoint `பல`, with scan 129 beginning `மணி நேரங்கள் கழிந்தன.`

## Material source-established results

### Scan 130 / printed 128

The baseline split the expression as `பயன்படுத்திக்கொள்வ தாக்கும்`. Native pixels establish the continuous source reading:

`பயன்படுத்திக்கொள்வதாக்கும்`

### Scan 131 / printed 129

Two substantive baseline discrepancies were corrected from source:

- source: `இதை நிச்சயமாகத் தெரிந்து கொள்!`
- source: `நிச்சயமாகத் தெரிகிறது—நீங்கள் பைத்தியக்காரர் இல்லை!`

The baseline omitted `கொள்` in the first reading and omitted `இல்லை` in the second.

### Scan 137 / printed 135

The claimed baseline range did not actually contain this page. Direct native transcription therefore restored the full physical page.

Protected source forms include:

- `பிறந்தத் தரணி—`;
- source-odd `புழுதி மண்ணுகப்`;
- physical endpoint `என் சோக வாழ்`.

The last word is physically incomplete at the page boundary. Its continuation belongs to scan 138 and is not guessed in scan 137.

## Physical continuity

- scan 128 `பல` → scan 129 `மணி நேரங்கள் கழிந்தன.`
- scan 132 quotation continues into scan 133.
- scan 134 final speech lead-in continues into scan 135.
- scan 137 ends at `வாழ்` and remains open into scan 138.

## Gate result

All newly created canonical records 129–137 are `verified`. No `needs-review` or `partial` item remains in this range.

The next canonical page is scan **138 / printed 136 / split part 003 page 40**, which is the known chapter **14 → 15** transition. Do not infer its text from scan 137; use the next user baseline against native source.
