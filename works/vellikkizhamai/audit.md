# Transcription / Source Audit — வெள்ளிக்கிழமை

## Current gate

| Check | Status |
|---|---|
| Source identity / checksum / page count | **complete** |
| Initial scan manifest | **179 / 179 represented** |
| Canonical page records | **138 / 179** |
| Body transcription | **through scan 138 / printed 137** |
| Printed-page map | **directly confirmed through scan 138; scan 66 visibly prints only `5`** |
| Chapter map | **direct through Chapter 17; Chapter 17 opens at scan 134** |
| Historical-glyph coverage | **PASS scans 1–138** |
| Forward transcription | **next 5-scan iteration 139–143** |
| Full Tamil source audit | **not started** |
| Assembled Tamil | **not started** |
| English translation | **blocked until Tamil gate passes** |

## Historical-glyph rule

Mandatory family set:

`ணா / ணை / ணொ / ணோ / லை / ளை / றா / றொ / றோ / னா / னை / னொ / னோ`

Character identity is decided from source pixels first; grammar is only a locator. Never global-replace or silently modernize source wording.

## Expanded corrective re-audit — controlling result

Earlier audit work incorrectly treated several old-type `னா` / `றா` shapes as genuine short-looking forms. Those classifications are superseded by the corrected canonical records and same-edition source comparison.

Representative durable corrections include scan 19 `வந்துவிட்டானா`; scan 23 `வயதினாள்`; scan 53 `பருவத்தினாள்`; scan 54 `விரும்பினான்`; scan 62 `பொய்தானா?`, `பேசினான்`; scan 63 `நோக்கினான்`; scan 90 `மாறினாள்`, `கத்தினாள்`; scan 91 `மாற்றினான்`, `கூறினான்`, `நிறுத்தினான்`; scan 92 `நீட்டினான்`, `துரிதப்படுத்தினான்`, `அமர்த்தினான்`; scan 93 `பொத்தினான்`; scan 95 `தொடங்கினாள்`; scan 96 `கிடத்தினான்`; scan 109 `கூறினான்`, `சொன்னான்`; scan 110 `புளுகினான்`; scan 112 `கருதினான்`; scan 113 `கூறினான்`; scan 114 `கதறினாள்`, `முனகினாள்`; scan 116 `தொடங்கினாள்`; scan 117 `கதறினாள்`, `கத்தினாள்`; scan 118 both `கிளம்பினாள்` occurrences.

These are character-identity corrections, not grammatical normalization. Independently source-confirmed `வேலையில்ல`, `வரணும்`, `நானு`, recurring `நயினா`, scan 114 `அப்படித்தான் ஆவள்`, and later source-specific spellings remain untouched.

## Coverage

- scans **1–101**: retrospective audit;
- scans **102–111**: corrective audit;
- scans **112–118**: forward audit;
- expanded corrective re-audit repaired old-type `னா` / `றா` misses across the existing corpus;
- scans **119–138**: forward source-pixel audit with the mandatory 13-family set;
- current canonical scans **1–138 are PASS**.

## Current forward end

### Scan 129 / printed 128
**VERIFIED / PASS.** Begins `அவளை`; source-specific `விழலுக்கிறைத்த`, `மனிதப்புழு`, `கழிசடை`, `சிந்துவாற்றுச்`; ends literal `அழகப்ப`.

### Scan 130 / printed 129
**VERIFIED / PASS.** Begins `னுடைய`, completing `அழகப்பனுடைய`; same-edition `நயினா`; preserve `மங்காகர`; bottom `9` printer/signature mark.

### Scan 131 / printed 130
**VERIFIED / PASS.** Same-edition `நயினாமுகம்மது`, `நயினாவின்`; ends literal `பின்னிக்`.

### Scan 132 / printed 131
**VERIFIED / PASS.** Begins `கொண்டன;`, completing `பின்னிக்கொண்டன`; `நயினாவிடமிருந்து`; both historical `என்றால்` source-confirmed.

### Scan 133 / printed 132
**VERIFIED / PASS.** `நயினா முகம்மது`, `நயினாவுக்கு`, `நகை நட்டுக்களை`; ends complete sentence.

### Scan 134 / printed 133
**VERIFIED / PASS. Mixed Chapter 16 / 17 boundary.** Centered `17`; preserve `முழுசம்`, `எழுதியிட்டான்`, `காபி சாப்பிட்டுக்கொண்டிருந்தவர்கள்`, `ததும்பக்கேட்டான்`; ends physical `நயினாவின்`.

### Scan 135 / printed 134
**VERIFIED / PASS.** Begins `கேள்வி`; preserve `பார்த்து.`, `செளக்கியந்தானே!`, `இளைப்பாயிருக்கிறதே`, `இப்படி யென்றால்`, `எதிர் காலத்தில்`; ends physical `வருத்தத்`.

### Scan 136 / printed 135
**VERIFIED / PASS.** Begins `தோடு`, completing `வருத்தத்தோடு`; preserve `சொல்லுகிறாளாக்கும்`, `பதிலக் கேள்வி`.

### Scan 137 / printed 136
**VERIFIED / PASS.** Preserve `சிக்கலங்கிப்போயிருக்கிறோம்`; ends inside open quotation `“இதோ,`.

### Scan 138 / printed 137
**VERIFIED / PASS.** Begins `கொண்டுவருகிறேன்”`, continuing scan 137; preserve source `வந்துவிட்டுமா?`; ends literal `‘மளமள’`.

## Structural/source decisions to preserve

- scan 33 Chapter 3 / 4, centered `4`;
- scan 45 Chapter 4 / 5, centered `5`;
- scan 52 Chapter 6 opening, centered `6`;
- scan 59 Chapter 6 / 7, centered `7`;
- scan 66 visibly prints only `5`; never infer `65`;
- scan 68 Chapter 7 / 8, centered `8`;
- scan 75 Chapter 8 / 9, centered `9`;
- scan 82 bottom `6` printer/signature mark;
- scan 85 Chapter 9 / 10, centered `10`;
- scan 89 / 90 physical `மறுத்துவிட்` + `டால்`;
- scan 92 Chapter 10 / 11, centered `11`;
- scan 98 bottom `7` printer/signature mark;
- scan 99 Chapter 11 / 12, centered `12`;
- scan 107 Chapter 12 / 13, centered `13`;
- scan 111 / 112 `சிறிதா` + `வது` = `சிறிதாவது`;
- scan 114 bottom `8` printer/signature mark;
- scan 115 Chapter 13 / 14, centered `14`;
- scan 117 / 118 literal source discontinuity `உட்` → `எவ்வளவோ...`;
- scan 119 / 120 `கவனித்து` → `விட்டான்.`;
- scan 120 Chapter 14 / 15, centered `15`;
- scan 122 / 123 literal boundary `...அவளுக்குப் பக்கத்திலே` → `கார்ந்து கொண்டாள்.`;
- scan 124 / 125 `புகை` + `வண்டி` = `புகைவண்டி`;
- scan 127 Chapter 16 opening, centered `16`;
- scan 127 / 128 `வாழ்` + `விலே` = `வாழ்விலே`;
- scan 129 / 130 `அழகப்ப` + `னுடைய` = `அழகப்பனுடைய`;
- scan 130 bottom `9` printer/signature mark;
- scan 131 / 132 `பின்னிக்` + `கொண்டன` = `பின்னிக்கொண்டன`;
- scan 134 Chapter 16 / 17 boundary, centered `17`;
- scan 134 / 135 `நயினாவின்` → `கேள்வி`;
- scan 135 / 136 `வருத்தத்` + `தோடு` = `வருத்தத்தோடு`;
- scan 137 / 138 open quotation `“இதோ,` → `கொண்டுவருகிறேன்”`;
- scan 138 ends literal `‘மளமள’`; no completion is inferred.

## Exact next activity

Process **scans 139–143** using the bounded production workflow: transcribe all five pages first, run one mandatory 13-family batch sweep, allow at most one targeted enlargement per genuinely unclear token, and record a source hold if still unresolved. Do not start assembled Tamil or English.
