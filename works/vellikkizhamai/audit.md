# Transcription / Source Audit — வெள்ளிக்கிழமை

## Current gate

| Check | Status |
|---|---|
| Source identity / checksum / page count | **complete** |
| Initial scan manifest | **179 / 179 represented** |
| Canonical page records | **118 / 179** |
| Body transcription | **through scan 118 / printed 117** |
| Printed-page map | **directly confirmed through scan 118; scan 66 visibly prints only `5`** |
| Chapter map | **direct through Chapter 14 at scan 118; Chapter 13 / 14 boundary scan 115** |
| Historical-glyph coverage on canonical scans | **PASS scans 1–118 after expanded corrective re-audit** |
| Forward transcription | **next 5-scan iteration 119–123** |
| Full Tamil source audit | **not started** |
| Assembled Tamil | **not started** |
| English translation | **blocked until Tamil gate passes** |

## Historical-glyph rule

Mandatory family set on every retrospective and forward scan:

`ணா / ணை / ணொ / ணோ / லை / ளை / றா / றொ / றோ / னா / னை / னொ / னோ`

Character identity is decided from enlarged/native source pixels first. Grammar is only a locator. Source spelling, grammar, punctuation, spacing and colloquial forms are preserved. Never global-replace.

## Expanded corrective re-audit — controlling result

The earlier audit incorrectly treated several old-type `னா` / `றா` shapes as genuine short-looking `னன்` / `னள்` / `னு` forms. Those classifications are superseded by direct same-edition pixel comparison and by the corrected canonical records committed through scan 118.

Durable corrected examples include:

- scan 19 `வந்துவிட்டானா`;
- scan 23 `வயதினாள்`;
- scan 53 `பருவத்தினாள்`;
- scan 54 `விரும்பினான்`;
- scan 62 `பொய்தானா?`, `பேசினான்`;
- scan 63 `நோக்கினான்`;
- scan 90 `மாறினாள்`, `கத்தினாள்`;
- scan 91 `மாற்றினான்`, `கூறினான்`, `நிறுத்தினான்`;
- scan 92 `நீட்டினான்`, `துரிதப்படுத்தினான்`, `அமர்த்தினான்`;
- scan 93 `பொத்தினான்`;
- scan 95 `தொடங்கினாள்`;
- scan 96 `கிடத்தினான்`;
- scan 109 `கூறினான்`, `சொன்னான்`;
- scan 110 `புளுகினான்`;
- scan 112 `கருதினான்`;
- scan 113 `கூறினான்`;
- scan 114 `கதறினாள்`, `முனகினாள்`;
- scan 116 `தொடங்கினாள்`;
- scan 117 `கதறினாள்`, `கத்தினாள்`;
- scan 118 both `கிளம்பினாள்` occurrences.

These are character-identity corrections, not blanket grammatical modernization. Source-specific forms that were independently confirmed remain untouched, including `வேலையில்ல`, `வரணும்`, `நானு`, recurring `நயினா`, and scan 114 `அப்படித்தான் ஆவள்`.

### Rule strengthened after the re-audit

An apparent `னன்` / `னள்` / `னு` in this edition must not be designated a genuine counterexample merely because the modern-looking shape appears short. It must be compared against same-edition old-type witnesses at enlarged/native resolution. Grammar may identify a candidate but never decides it.

## Historical-glyph coverage through scan 118

- scans **1–101**: retrospective audit performed;
- scans **102–111**: corrective audit performed;
- scans **112–118**: forward source-pixel audit performed;
- expanded corrective re-audit subsequently repaired the missed old-type `னா` / `றா` identities across the existing corpus;
- current canonical records **1–118 are PASS after those corrections**.

## Forward canonical processing — current end

### Scan 112 / printed 111

**VERIFIED / expanded historical-glyph PASS.** Opening `வது` completes scan 111 `சிறிதா`, forming cross-page `சிறிதாவது`. Corrected source reading: `கருதினான்`. Preserve `திருமணந்தான்`, `மனத்திலேயிருந்த`, `அனுதாபந்தான்`.

### Scan 113 / printed 112

**VERIFIED / expanded historical-glyph PASS.** Corrected source reading: `கூறினான்`. Preserve `பிடிகை`, `கூளம்`, `தவங்கிடந்து`, `அவையடக்கம்போலவே`. Final `ஆறுதல் கூறுவதற்கு ஒருவர் அருகே இருக்கும்போது` continues on scan 114 with `பாசமும் பற்றும்...`.

### Scan 114 / printed 113

**VERIFIED / expanded historical-glyph PASS.** Preserve source-specific `அப்படித்தான் ஆவள்`, `சீழ்`, `அமராழ்`, `அசைவற்றுவிட்டான்`; corrected `கதறினாள்`, `முனகினாள்`. Bottom standalone `8` is a printer/signature mark.

### Scan 115 / printed 114

**VERIFIED / expanded historical-glyph PASS. Mixed Chapter 13 / 14 boundary.** Centered `14`; preserve `சூடான`, source spacing `கண்டு பிடித்த`, `நமைச்சலுக்கு`, `திருமணமென்றும்`.

### Scan 116 / printed 115

**VERIFIED / expanded historical-glyph PASS.** Preserve `தவறாகக்`, `கட்டுக் குலைந்து`, `சித்திரப் புள்`, `பூர்வ ஜென்ம பூஜாபலன்`, `புத்தியைத் தீட்ட`; corrected `தொடங்கினாள்`. Ends `நான் யாருக்கு என்ன துரோகம்`, continued on scan 117.

### Scan 117 / printed 116

**VERIFIED / expanded historical-glyph PASS.** Corrected `கதறினாள்`, `கத்தினாள்`; preserve `சவத்திற்குக்கூட`, `அபிப்பிராயங்`, `நம்ப`, `கல்லெடுப்பும்`. The physical scan ends with literal source `உட்`; no completion is invented.

### Scan 118 / printed 117

**VERIFIED / expanded historical-glyph PASS.** Corrected both `கிளம்பினாள்` occurrences. Begins `எவ்வளவோ முயன்றும் நடக்கவில்லை.` without supplying text for scan 117's final `உட்`; preserve `களங்கப் பனியை`, `பத்தரைமாற்றுத்`, `கல்யாணந்தானே`, `அகற்றியாகவேண்டும்`, `தணலாயிருக்கிறது`. Final dialogue continues to scan 119 with `வருகிறேன்.........`.

## Structural decisions to preserve

- scan 33 mixed Chapter 3 / 4, centered `4`;
- scan 45 mixed Chapter 4 / 5, centered `5`;
- scan 52 Chapter 6 opening, centered `6`;
- scan 59 mixed Chapter 6 / 7, centered `7`;
- scan 66 visibly prints only `5`; never infer `65`;
- scan 68 mixed Chapter 7 / 8, centered `8`;
- scan 75 mixed Chapter 8 / 9, centered `9`;
- scan 82 bottom `6` printer/signature mark;
- scan 85 mixed Chapter 9 / 10, centered `10`;
- scan 89 / 90 physical split `மறுத்துவிட்` + `டால்`;
- scan 92 mixed Chapter 10 / 11, centered `11`;
- scan 98 bottom `7` printer/signature mark;
- scan 99 mixed Chapter 11 / 12, centered `12`;
- scan 101 / 102 continuation;
- scan 107 mixed Chapter 12 / 13, centered `13`;
- scan 111 / 112 physical split `சிறிதா` + `வது` = `சிறிதாவது`;
- scan 113 / 114 sentence continuation;
- scan 114 bottom `8` printer/signature mark;
- scan 115 mixed Chapter 13 / 14, centered `14`;
- scan 116 / 117 sentence continuation;
- scan 117 / 118 literal `உட்` → `எவ்வளவோ...` source discontinuity;
- scan 118 / 119 open-dialogue continuation.

## Exact next activity

Process **scans 119–123** as the next 5-page forward iteration. Apply the mandatory 13-family historical-glyph check directly from source pixels before encoding each page. Preserve the scan 118 / 119 continuation beginning `வருகிறேன்.........`. Do not start assembled Tamil or English.
