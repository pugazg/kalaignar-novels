# Transcription / Source Audit — வெள்ளிக்கிழமை

## Current gate

| Check | Status |
|---|---|
| Source identity / checksum / page count | **complete** |
| Initial scan manifest | **179 / 179 represented** |
| Canonical page records | **133 / 179** |
| Body transcription | **through scan 133 / printed 132** |
| Printed-page map | **directly confirmed through scan 133; scan 66 visibly prints only `5`** |
| Chapter map | **direct through Chapter 16 at scan 133; Chapter 16 opens on scan 127** |
| Historical-glyph coverage on canonical scans | **PASS scans 1–133** |
| Forward transcription | **next 5-scan iteration 134–138** |
| Full Tamil source audit | **not started** |
| Assembled Tamil | **not started** |
| English translation | **blocked until Tamil gate passes** |

## Historical-glyph rule

Mandatory family set on every retrospective and forward scan:

`ணா / ணை / ணொ / ணோ / லை / ளை / றா / றொ / றோ / னா / னை / னொ / னோ`

Character identity is decided from enlarged/native source pixels first. Grammar is only a locator. Source spelling, grammar, punctuation, spacing and colloquial forms are preserved. Never global-replace.

## Expanded corrective re-audit — controlling result

The earlier audit incorrectly treated several old-type `னா` / `றா` shapes as genuine short-looking `னன்` / `னள்` / `னு` forms. Those classifications are superseded by direct same-edition pixel comparison and by the corrected canonical records.

Durable corrected examples include scan 19 `வந்துவிட்டானா`; scan 23 `வயதினாள்`; scan 53 `பருவத்தினாள்`; scan 54 `விரும்பினான்`; scan 62 `பொய்தானா?`, `பேசினான்`; scan 63 `நோக்கினான்`; scan 90 `மாறினாள்`, `கத்தினாள்`; scan 91 `மாற்றினான்`, `கூறினான்`, `நிறுத்தினான்`; scan 92 `நீட்டினான்`, `துரிதப்படுத்தினான்`, `அமர்த்தினான்`; scan 93 `பொத்தினான்`; scan 95 `தொடங்கினாள்`; scan 96 `கிடத்தினான்`; scan 109 `கூறினான்`, `சொன்னான்`; scan 110 `புளுகினான்`; scan 112 `கருதினான்`; scan 113 `கூறினான்`; scan 114 `கதறினாள்`, `முனகினாள்`; scan 116 `தொடங்கினாள்`; scan 117 `கதறினாள்`, `கத்தினாள்`; scan 118 both `கிளம்பினாள்` occurrences.

These are character-identity corrections, not blanket grammatical modernization. Source-specific forms that were independently confirmed remain untouched, including `வேலையில்ல`, `வரணும்`, `நானு`, recurring `நயினா`, scan 114 `அப்படித்தான் ஆவள்`, and source-specific clusters on scans 119–133.

An apparent `னன்` / `னள்` / `னு` must be compared against same-edition old type at enlarged/native resolution before it can be called genuine.

## Historical-glyph coverage through scan 133

- scans **1–101**: retrospective audit performed;
- scans **102–111**: corrective audit performed;
- scans **112–118**: forward audit performed;
- expanded corrective re-audit repaired the subsequently discovered old-type `னா` / `றா` misses across the existing corpus;
- scans **119–133**: forward source-pixel audit performed with the mandatory 13-family set;
- current canonical records **1–133 are PASS**.

## Forward canonical processing — current end

### Scan 119 / printed 118
**VERIFIED / historical-glyph PASS.** Source-visible scan-118 continuation `வருகிறேன்.........`; `ஓடினாள்` confirmed. Preserve `பதைக்`, `வஞ்சங்`, `நச்சரவு`, `ஜாடையாக`. Ends `கவனித்து`.

### Scan 120 / printed 119
**VERIFIED / historical-glyph PASS. Mixed Chapter 14 / 15 boundary.** Begins `விட்டான்.`; centered `15`; `நின்றாள்` and one source-visible `எண்ணினான்` confirmed. The previous target note saying “both `எண்ணினான்`” is superseded; the other nearby source form is `ஏங்கினான்`.

### Scan 121 / printed 120
**VERIFIED / historical-glyph PASS.** `குமுறினாள்`, `அலறினாள்`, both `ஓடினான்` confirmed. Preserve `கரந்தொட்டுப்`, `ஆத்திரமனைத்தையும்`, `அவன்கையில்`, `புறப்படத்தயாராக`. Ends `வண்டியில்`.

### Scan 122 / printed 121
**VERIFIED / historical-glyph PASS.** `வருகிறானா`, `போகிறாள்`, `கிளம்பியிருக்கிறாள்` confirmed. Preserve `தீமரென`, `தொத்திக்கொண்டான்`, `பலகணி`, and the printed jewelry phrase. Ends `சிந்தாமணி, அவளுக்குப் பக்கத்திலே`.

### Scan 123 / printed 122
**VERIFIED / historical-glyph PASS.** `நோக்கினாள்`, `நன்றாகக்`, `தோன்றினாள்` confirmed. Disputed lexical clusters closed from source pixels: literal opening `கார்ந்து கொண்டாள்.`, `மாது சிரோன்மணி`, `தண்டனைகளையெல்லாம்விட`, `கதவண்டை`. The 122 / 123 boundary is not repaired to `உட்கார்ந்து`.

### Scan 124 / printed 123
**VERIFIED / historical-glyph PASS.** `வயதானவளை`, `ஏற்றிக்கொண்டிருந்தான்` source-confirmed. Ends literal `புகை`.

### Scan 125 / printed 124
**VERIFIED / historical-glyph PASS.** Begins `வண்டி`, completing physical `புகைவண்டி`; preserve `சூடேறிவிட்டது`, `வனைத்`, `அழகப்பனைத்`, `சேச்சே`.

### Scan 126 / printed 125
**VERIFIED / historical-glyph PASS.** Preserve `நாலைந்து`, `திடுதிடுவென`, `கதவண்டை`, `ஏறினார்கள்`, `கோதைக்கு`.

### Scan 127 / printed 126
**VERIFIED / historical-glyph PASS. Chapter 16 opening.** Centered `16`; preserve `புறக்கூடு`, `புடைசூழ`, `நிலை குலறியது`; ends literal `வாழ்`.

### Scan 128 / printed 127
**VERIFIED / historical-glyph PASS.** Begins `விலே`, completing `வாழ்விலே`; preserve `அணுஅணுவாகக்`, `வலியப்போய்`, `அவனைச் சூழ்ந்தது`; ends `ஏறும்போது`.

### Scan 129 / printed 128
**VERIFIED / historical-glyph PASS.** Directly continues scan 128 with `அவளை`; preserve `விழலுக்கிறைத்த`, `மனிதப்புழு`, `கழிசடை`, `சிந்துவாற்றுச்`; ends literal `அழகப்ப`.

### Scan 130 / printed 129
**VERIFIED / historical-glyph PASS.** Begins `னுடைய`, completing `அழகப்பனுடைய`; historical comparison confirms `நயினா`; preserve `மங்காகர`, `துயிலுற்றி யெழுந்தாள்`, `எண்ணங்களே முழங்கிக்கொண்டிருந்தன`; bottom `9` printer/signature mark.

### Scan 131 / printed 130
**VERIFIED / historical-glyph PASS.** Same-edition `நயினாமுகம்மது`, `நயினாவின்`; source-confirmed `என்றாள்`, `மாறாமலிருந்தால்`; ends physical `பின்னிக்`.

### Scan 132 / printed 131
**VERIFIED / historical-glyph PASS.** Begins `கொண்டன;` → cross-page `பின்னிக்கொண்டன`; `நயினாவிடமிருந்து` and both `என்றால்` confirmed from historical type; preserve `நிரம்பப் படித்த`, `வேட்டாக்கர்`.

### Scan 133 / printed 132
**VERIFIED / historical-glyph PASS.** Historical identities confirm `நயினா முகம்மது`, `நயினாவுக்கு`, `நகை நட்டுக்களை`; preserve `தடு மாறியவாறு`, `கம்பி நீட்டி விட்டான்`, `நோட்டுக்கத்தையைக்`, `வெளியிற்சென்று`.

## Structural decisions to preserve

- scan 33 mixed Chapter 3 / 4, centered `4`;
- scan 45 mixed Chapter 4 / 5, centered `5`;
- scan 52 Chapter 6 opening, centered `6`;
- scan 59 mixed Chapter 6 / 7, centered `7`;
- scan 66 visibly prints only `5`; never infer `65`;
- scan 68 mixed Chapter 7 / 8, centered `8`;
- scan 75 mixed Chapter 8 / 9, centered `9`;
- scan 82 bottom `6` printer/signature mark;
- scan 85 mixed Chapter 9 / 10, centered `10`;
- scan 89 / 90 physical split `மறுத்துவிட்` + `டால்`;
- scan 92 mixed Chapter 10 / 11, centered `11`;
- scan 98 bottom `7` printer/signature mark;
- scan 99 mixed Chapter 11 / 12, centered `12`;
- scan 107 mixed Chapter 12 / 13, centered `13`;
- scan 111 / 112 physical split `சிறிதா` + `வது` = `சிறிதாவது`;
- scan 114 bottom `8` printer/signature mark;
- scan 115 mixed Chapter 13 / 14, centered `14`;
- scan 117 / 118 literal `உட்` → `எவ்வளவோ...` source discontinuity;
- scan 118 / 119 open-dialogue continuation;
- scan 119 / 120 `கவனித்து` → `விட்டான்.`;
- scan 120 mixed Chapter 14 / 15, centered `15`;
- scan 121 / 122 `வண்டியில்` → `ஒரே கூட்டம்.`;
- scan 122 / 123 literal `...அவளுக்குப் பக்கத்திலே` → `கார்ந்து கொண்டாள்.`;
- scan 124 / 125 `புகை` + `வண்டி` = `புகைவண்டி`;
- scan 127 Chapter 16 opening, centered `16`;
- scan 127 / 128 `வாழ்` + `விலே` = `வாழ்விலே`;
- scan 128 ends physical `ஏறும்போது`.
- scan 128 / 129 direct continuation `ஏறும்போது` → `அவளை...`;
- scan 129 / 130 `அழகப்ப` + `னுடைய` = `அழகப்பனுடைய`;
- scan 130 bottom `9` printer/signature mark;
- scan 131 / 132 `பின்னிக்` + `கொண்டன` = `பின்னிக்கொண்டன`;
- scan 133 ends complete sentence.

## Exact next activity

Process **scans 134–138** as the next 5-page forward iteration. Use the bounded production workflow: full-page transcription of all five first, one batch-level 13-family glyph sweep, and at most one targeted enlargement per genuinely unclear token before recording a source hold. Preserve the durable scan 129 / 130 and scan 131 / 132 physical splits; establish scan 134 only from direct source evidence. Do not start assembled Tamil or English.
