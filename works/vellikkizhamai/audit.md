# Transcription / Source Audit — வெள்ளிக்கிழமை

## Current gate

| Check | Status |
|---|---|
| Source identity / checksum / page count | **complete** |
| Initial scan manifest | **179 / 179 represented** |
| Canonical page records | **111 / 179** |
| Body transcription | **through scan 111** |
| Printed-page map | **directly confirmed through scan 111; scan 66 visibly prints only `5`** |
| Chapter map | **direct through Chapter 13 continuation at scan 111** |
| Historical-glyph work-level gate | **REOPENED** |
| Corrective historical-glyph re-audit | **PASS scans 102–111** |
| Retrospective historical-glyph re-audit | **PASS scans 1–40; PENDING scans 41–101** |
| Full Tamil source audit | **not started** |
| Assembled Tamil | **not started** |
| English translation | **blocked until Tamil gate passes** |

## Historical-glyph rule

Every retrospective page is checked directly against `ணா / ணை / ணொ / ணோ / லை / ளை / றா / றொ / றோ / னா / னை / னொ / னோ`. Character identity comes from enlarged/native source pixels. Grammar can locate a suspicious form but is never evidence. Source spelling, grammar, punctuation and colloquial usage are preserved after character identity is decoded. No global replacement is allowed.

## Retrospective coverage

### Scans 1–5 — PASS

No historical-glyph correction was required. Representative confirmations: scan 1 `கருணாநிதி` (`ணா`) / `திராவிடப்பண்ணை` (`ணை`), scan 3 `விலை` (`லை`), scan 4 `அன்னைப்` / `மாவீரனைப்` (`னை`) and `போனான்` (`னா`), scan 5 `சாணமிட்டு` (`ணா`) / `நன்றாக` (`றா`). Separate ordinary source comparison corrected scan 4 `பெருகிடுகிறது` → **`பெருகிவிடுகிறது`**.

### Scans 6–10 — PASS

No canonical correction. Confirmed examples include `என்னை`, `மறுவேலை`, `உளறுகிறேனா`, `தூங்காமலிருந்திருக்கிறோமே`, `களைத்துத்`, `போறான்`, `புறாக்கள்`, `கொடியன்றோ`, `தினந்தோறும்`, `நாணாதாய்`, and `எங்களை`. Source-specific `அய் யோன்னு` and `குழப்பட்டு` were retained.

### Scans 11–15 — PASS

No canonical correction. Confirmed examples include `இத்தனை`, `கண்களை`, `துயிலணைமேல்`, `நன்றாகத்`, `என்றாள்`, `தாமரையானாள்`, `மாப்பிள்ளை`, `எத்துணை`, `பெற்றோர்களால்`, `நின்றாள்`, `அர்ச்சுனனை`, and `நன்னாள்`.

### Scans 16–20 — PASS

No canonical correction. High-risk colloquial forms scan 17 `வேலையில்ல` and scan 19 `வந்துவிட்டானு` were independently rechecked and retained exactly as printed.

### Scans 21–25 — PASS

No canonical correction. Scan 21 `வரணும்` is genuine `ணு`; scan 23 `வயதினள்` is genuine `னள்`, not `வயதினாள்`. Other representative families were confirmed across `கல்யாணம்`, `போறான்`, `அவளை`, `பேச்சில்லை`, `பிணைக்கப்படுவார்கள்`, `பெற்றோர்கள்`, and `டைகரானான்`.

### Scans 26–30 — PASS

No canonical correction. The uncommon source identities scan 27 `காணோமே` (`ணோ`) and scan 30 `பெண்ணொருத்தி` (`ணொ`) were explicitly resolved from source pixels.

### Scans 31–35 — PASS

No canonical correction. Scan 31 `அவ்விதமானாள்` is source-supported `னா`; scan 33 line-broken `நட்டு` + `வனார்` resolves to `நட்டுவனார்`; scans 34–35 independently reconfirm recurring `நயினா`, not `நயினு`.

### Scans 36–40 — PASS after 4 corrections

All five scans were directly inspected against the complete mandatory family set.

| Scan | Source-supported result |
|---:|---|
| 36 | `நயினா` / `மாமனார்` (`னா`), `பெண்ணைப்` (`ணை`), `கவலைப்படாமல்` (`லை`), `எங்களைப்போல` (`ளை`), `மாறுதல்` (`றா`) confirmed; no correction |
| 37 | `பெண்ணைச்` (`ணை`), `மாப்பிள்ளைக்கு` (`ளை`), `கல்யாணத்துக்குச்` (`ணா`), `நயினா` / `மாமனார்` (`னா`), `புண்ணியமில்லை` (`லை`), `மாற்ற` (`றா`) confirmed; no correction |
| 38 | **`கண்ணின் மிகுதியால்` → `களைப்பின் மிகுதியால்`** (`ளை`) |
| 39 | `மனைவி` (`னை`), `சாலையில்` (`லை`), `நயினா` (`னா`), `வளைத்துக்கொண்டார்கள்` (`ளை`) confirmed; no correction |
| 40 | **`எண்ணையாவது` → `என்னையாவது`** (`னை`); **`அவர்களோ` → `அவர்களை`** (`ளை`); **`கைவற்றுப்போனேன்` → `கைவற்றுப்போனான்`** (`னா`) |

No global replacement, punctuation repair or silent modernization was used in scans 36–40.

## Corrective re-audit — scans 102–111

This range remains PASS after the earlier systematic corrective pass. Confirmed repairs include `ஆட்டினாள்`, `நன்றாகக்`, `கத்தினாள்`, `கூறினாள்`, `திருப்பினாள்`, `என்றாள்`, `தவறாக`, `கொட்டினான்`, `ஓடினான்`, `ஓடினார்கள்`, `திமிறினான்`, `நடுங்கினாள்`, `சொன்னா`, `பொய்தானா?`, and `பெண்ணா?`. Scan 105 and scan 111 required no correction. Scan 109 `கூறினன்` / `சொன்னன்` and scan 110 `புளுகினன்` were independently rechecked and retained.

## Structural decisions to preserve

- recurring name: **`நயினா`**, not `நயினு`;
- scan 23 `வயதினள்` is genuine;
- scan 27 `காணோமே` is `ணோ`;
- scan 30 `பெண்ணொருத்தி` is `ணொ`;
- scan 31 `அவ்விதமானாள்` is source-confirmed;
- scan 33 `நட்டுவனார்` is source-confirmed;
- scan 66 visibly prints only `5`; never infer `65`;
- scan 98 bottom standalone `7` is a printer/signature mark;
- scan 99 is mixed Chapter 11 / Chapter 12 with centered `12`;
- scan 107 is mixed Chapter 12 / Chapter 13 with centered `13`;
- scan 111 ends physical `சிறிதா`; scan 112 starts source-visible `வது`, yielding `சிறிதாவது` across the physical boundary.

## Unresolved project items

- retrospective historical-glyph re-audit **scans 41–101**;
- forward canonical processing **scans 112–179**;
- final complete Tamil source audit;
- assembled Tamil;
- English after the Tamil gate passes.

## Exact next activity

Do **not** resume forward transcription yet. Process **scans 41–45** as the next retrospective 5-scan historical-glyph audit batch. Correct only positively source-supported character identities, synchronize status records, and continue chronologically.
