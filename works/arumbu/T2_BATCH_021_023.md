# T2 Independent Historical-Glyph Re-read — அரும்பு scans 21–23

Checkpoint: **T2 — independent historical-glyph / character-identity re-read**  
Status: **PASS / COMPLETE**  
Source: `TVA_BOK_0064361_அரும்பு.pdf`

## Scope

Final physical scans **21–23** of the `அரும்பு` component, after T1 was durably completed.

Authority: direct enlarged/native source pixels plus the repository `HISTORICAL_TAMIL_GLYPH_TRANSCRIPTION_GUIDE.md`. No OCR or contextual normalization was used as authority.

## Mandatory family coverage

Each complete page was independently re-read and the full known set was explicitly checked:

`ணா / ணை / ணொ / ணோ / லை / ளை / றா / றொ / றோ / னா / னை / னொ / னோ`

Families not directly present on a page were recorded as absent rather than inferred. Same-edition comparison was used only where the source form warranted confirmation.

## Result

| Scan | Printed page | T2 character-identity corrections | Unresolved historical glyphs | Result |
|---:|:---:|---:|---:|---|
| 21 | 17 | 0 | 0 | PASS |
| 22 | 18 | 0 | 0 | PASS |
| 23 | 19 | 0 | 0 | PASS |

**T2 corrections: 0.**  
**Unresolved historical glyphs: 0.**

## Representative source-confirmed identities

- scan 21: `அணைத்துக்கொண்டான்` (`ணை`), `கண்ணாடித்` (`ணா`), `இல்லை` (`லை`), `பிள்ளைதான்` (`ளை`), `நன்றாகத்` (`றா`);
- scan 22: `வேலை` / `காலைப்` (`லை`), `அவனைச்` / `மனைவியின்` (`னை`), `தன்னாலேயே` (`னா`);
- scan 23: `கண்ணாடித்` (`ணா`), `இலைகளும்` / `காலைக்` (`லை`), `பிள்ளையைப்` / `பிள்ளையிடம்தான்` (`ளை`), `புறாக்களில்` (`றா`), `அம்மனார்` (`னா`), `தன்னை` (`னை`).

## Explicit re-checks from T1

- scan 22 `பேசினேன்` — enlarged source pixels directly confirm this printed wording; retained unchanged. It is not context-normalized.
- scan 23 `அம்மனார்` — enlarged source pixels directly confirm this printed wording and `னா` identity; retained unchanged.

These confirmations are source-fidelity observations, not permission to modernize or lexically rewrite the text.

## Gate result

**T2 — PASS / COMPLETE for scans 21–23.** All three canonical records remain `needs-review` because final T3 source-fidelity closure has not yet been performed.

Current component state:

- canonical records: **18 / 18**;
- verified records: **15 / 18 — scans 6–20**;
- scans 21–23 T1: **PASS / COMPLETE**;
- scans 21–23 T2: **PASS / COMPLETE — 0 corrections / 0 unresolved**;
- scans 21–23 T3: **NEXT**.

## Exact next activity

Run **T3 — final source-fidelity closure for scans 21–23 only**: independently compare the complete pages against direct source pixels for omissions, unsupported text, punctuation, page joins, printed-page visibility and non-body separation; retain the T2-confirmed source readings unless the pixels directly prove otherwise; synchronize controls, commit T3, and stop before beginning `சாரப்பள்ளம் சாமுண்டி`.
